version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - git submodule update
      - run: make
      - run: ./test
      - run: ls -l test-dev*
      
