version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: git submodule update --init
      - run: make
      - run: ./test
      - run: ls -l test-dev*
      - run: make -C test-devel1
      - run: test-devel1/test


workflows:
  version: 2
  build_and_test:
    jobs:
      - build

