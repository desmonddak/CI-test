version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: git submodule update --init
      - run: make
      - run: ./test
      - run: ls -l test-dev*
      
